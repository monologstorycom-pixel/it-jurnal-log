<style>
  /* ===== BOOTSTRAP ICONS ===== */
  @import url('https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css');

  /* ===== LUCIDE ICON STYLE ===== */
  .lc { display:inline-block; vertical-align:-0.15em; width:1em; height:1em; stroke:currentColor; fill:none; stroke-width:2; stroke-linecap:round; stroke-linejoin:round; flex-shrink:0; }
  .lc-sm { width:0.75em !important; height:0.75em !important; }
  .lc-lg { width:1.3em !important; height:1.3em !important; }
  /* stat card icons */
  .stat-card-icon { display:flex; align-items:center; justify-content:center; }
  .stat-card-icon .lc { width:1.4em; height:1.4em; }
  /* empty state */
  .empty-state-icon { display:flex; align-items:center; justify-content:center; }
  .empty-state-icon .lc { width:2.2em; height:2.2em; opacity:0.35; }
  /* label headings with icon */
  .f-label .lc, label .lc { width:0.9em; height:0.9em; vertical-align:-0.1em; }
  /* modal title icons */
  .modal-title .lc { width:0.9em; height:0.9em; vertical-align:-0.1em; }
  /* button icons */
  button .lc, a .lc { vertical-align:-0.15em; }

  /* ===== GLOBAL THEME VARS ===== */
  :root {
    --nav-bg: #ffffff;
    --nav-border: rgba(0,0,0,0.08);
    --nav-text: #111827;
    --nav-text2: #6b7280;
    --nav-hover-bg: #f9fafb;
    --nav-active-bg: #f3f4f6;
    --body-bg: #f9fafb;
    --body-bg2: #ffffff;
    --body-bg3: #f3f4f6;
    --body-text: #111827;
    --body-text2: #6b7280;
    --body-text3: #9ca3af;
    --body-border: rgba(0,0,0,0.08);
    --body-border2: rgba(0,0,0,0.14);
    --body-card: #ffffff;
    --body-input: #f9fafb;
    --body-input-border: rgba(0,0,0,0.1);
    --body-input-focus: #6366f1;
    --body-accent: #16a34a;
    --body-accent-bg: #dcfce7;
    --body-err-bg: #fff0f0;
    --body-err-text: #dc2626;
    --body-err-border: #fca5a5;
    --body-table-row: #ffffff;
    --body-table-row-alt: #f9fafb;
    --body-table-text: #111827;
    --body-table-border: rgba(0,0,0,0.07);
    --body-table-head: #f9fafb;
    --body-table-head-text: #374151;
    --avatar-bg: #dcfce7;
    --avatar-text: #15803d;
    --role-admin-bg: #ede9fe;
    --role-admin-text: #6d28d9;
    --role-user-bg: #dcfce7;
    --role-user-text: #15803d;
    --role-viewer-bg: #dbeafe;
    --role-viewer-text: #1d4ed8;
  }

  [data-theme="dark"] {
    --nav-bg: #0f1117;
    --nav-border: rgba(255,255,255,0.07);
    --nav-text: #f1f5f9;
    --nav-text2: #94a3b8;
    --nav-hover-bg: #1e2130;
    --nav-active-bg: #1a1d2e;
    --body-bg: #080a10;
    --body-bg2: #0f1117;
    --body-bg3: #13161f;
    --body-text: #e2e8f0;
    --body-text2: #8b8fa8;
    --body-text3: #3b4263;
    --body-border: rgba(255,255,255,0.07);
    --body-border2: rgba(255,255,255,0.12);
    --body-card: #0f1117;
    --body-input: #080a10;
    --body-input-border: rgba(255,255,255,0.08);
    --body-input-focus: #818cf8;
    --body-accent: #4ade80;
    --body-accent-bg: #0d2a18;
    --body-err-bg: #2a0d0d;
    --body-err-text: #f87171;
    --body-err-border: #5c1a1a;
    --body-table-row: #0f1117;
    --body-table-row-alt: #13161f;
    --body-table-text: #e2e8f0;
    --body-table-border: rgba(255,255,255,0.07);
    --body-table-head: #1a1e2a;
    --body-table-head-text: #94a3b8;
    --avatar-bg: #0d2a18;
    --avatar-text: #4ade80;
    --role-admin-bg: #1a0d3d;
    --role-admin-text: #a78bfa;
    --role-user-bg: #0d2a18;
    --role-user-text: #4ade80;
    --role-viewer-bg: #0d1a38;
    --role-viewer-text: #7eb8f7;
  }

  body {
    background-color: var(--body-bg) !important;
    color: var(--body-text) !important;
    transition: background-color 0.2s, color 0.2s;
  }

  /* ===== NAVBAR ===== */
  .it-nav {
    position: sticky;
    top: 0;
    z-index: 999;
    background: var(--nav-bg);
    border-bottom: 0.5px solid var(--nav-border);
    height: 56px;
    display: flex;
    align-items: center;
    padding: 0 24px;
    gap: 0;
    transition: background 0.2s, border-color 0.2s;
  }

  /* BRAND */
  .it-nav-brand {
    font-size: 13px;
    font-weight: 600;
    color: var(--nav-text);
    letter-spacing: 0.03em;
    text-decoration: none;
    margin-right: 28px;
    white-space: nowrap;
    flex-shrink: 0;
  }
  .it-nav-brand span { color: var(--body-accent); }

  /* LINKS */
  .it-nav-links {
    display: flex;
    align-items: center;
    gap: 2px;
    flex: 1;
  }
  .it-nav-link {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 6px 12px;
    border-radius: 8px;
    font-size: 13px;
    color: var(--nav-text2);
    text-decoration: none;
    cursor: pointer;
    transition: background 0.12s, color 0.12s;
    white-space: nowrap;
    line-height: 1;
  }
  .it-nav-link:hover { background: var(--nav-hover-bg); color: var(--nav-text); }
  .it-nav-link.active { background: var(--nav-active-bg); color: var(--nav-text); font-weight: 500; }
  .it-nav-link svg { width: 14px; height: 14px; flex-shrink: 0; }

  /* RIGHT SIDE */
  .it-nav-right {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-left: auto;
    flex-shrink: 0;
  }

  /* DIVIDER */
  .it-nav-divider {
    width: 0.5px;
    height: 20px;
    background: var(--nav-border);
    margin: 0 2px;
  }

  /* THEME TOGGLE */
  .it-nav-theme {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 5px 10px;
    border-radius: 20px;
    border: 0.5px solid var(--nav-border);
    cursor: pointer;
    font-size: 12px;
    color: var(--nav-text2);
    background: transparent;
    transition: border-color 0.12s, color 0.12s;
  }
  .it-nav-theme:hover { border-color: var(--body-border2); color: var(--nav-text); }
  .t-track {
    width: 26px;
    height: 14px;
    background: var(--nav-border);
    border-radius: 7px;
    position: relative;
    transition: background 0.2s;
    flex-shrink: 0;
  }
  .t-thumb {
    position: absolute;
    top: 2px;
    left: 2px;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--nav-text2);
    transition: transform 0.2s, background 0.2s;
  }
  [data-theme="light"] .t-track { background: #16a34a; }
  [data-theme="light"] .t-thumb { transform: translateX(12px); background: #ffffff; }

  /* USER PILL + DROPDOWN */
  .it-nav-user-wrap { position: relative; }
  .it-nav-user {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 4px 10px 4px 4px;
    border-radius: 24px;
    border: 0.5px solid var(--nav-border);
    cursor: pointer;
    transition: background 0.12s;
    user-select: none;
  }
  .it-nav-user:hover { background: var(--nav-hover-bg); }
  .it-user-dropdown {
    display: none;
    position: absolute;
    right: 0;
    top: calc(100% + 8px);
    background: var(--body-card, #1a1d27);
    border: 1px solid var(--body-border, #2a2d3a);
    border-radius: 12px;
    min-width: 180px;
    box-shadow: 0 8px 30px rgba(0,0,0,0.35);
    z-index: 9999;
    overflow: hidden;
    animation: ddFadeIn 0.15s ease;
  }
  @keyframes ddFadeIn { from { opacity: 0; transform: translateY(-6px); } to { opacity: 1; transform: none; } }
  .it-user-dropdown.open { display: block; }
  .it-dd-header { padding: 12px 14px 10px; border-bottom: 1px solid var(--body-border, #2a2d3a); }
  .it-dd-name { font-size: 0.82rem; font-weight: 700; color: var(--body-text, #e2e8f0); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .it-dd-username { font-size: 0.72rem; color: var(--body-text3, #6b7280); margin-top: 2px; }
  .it-dd-item {
    display: flex; align-items: center; gap: 9px;
    padding: 10px 14px;
    font-size: 0.8rem; font-weight: 600;
    color: var(--body-text2, #94a3b8);
    text-decoration: none;
    transition: background 0.12s, color 0.12s;
    cursor: pointer; border: none; background: none; width: 100%; text-align: left;
  }
  .it-dd-item:hover { background: var(--body-bg3, #1e2433); color: var(--body-text, #e2e8f0); }
  .it-dd-item.danger { color: #dc2626; }
  .it-dd-item.danger:hover { background: rgba(220,38,38,0.08); color: #dc2626; }
  .it-dd-divider { height: 1px; background: var(--body-border, #2a2d3a); margin: 2px 0; }

  /* AVATAR */
  .it-nav-avatar {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--avatar-bg);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 10px;
    font-weight: 600;
    color: var(--avatar-text);
    flex-shrink: 0;
    letter-spacing: 0.03em;
    overflow: hidden;
  }
  .it-nav-avatar img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
  .it-nav-name {
    font-size: 13px;
    font-weight: 500;
    color: var(--nav-text);
    white-space: nowrap;
  }

  /* ROLE BADGE */
  .it-nav-role {
    font-size: 10px;
    font-weight: 600;
    padding: 2px 7px;
    border-radius: 10px;
    white-space: nowrap;
    letter-spacing: 0.04em;
  }
  .role-admin  { background: var(--role-admin-bg);  color: var(--role-admin-text); }
  .role-user   { background: var(--role-user-bg);   color: var(--role-user-text); }
  .role-viewer { background: var(--role-viewer-bg); color: var(--role-viewer-text); }
  .role-custom { background: var(--role-viewer-bg); color: var(--role-viewer-text); }

  /* LOGOUT BTN */
  .it-nav-logout {
    padding: 5px 12px;
    border-radius: 8px;
    border: 0.5px solid var(--nav-border);
    font-size: 12px;
    font-weight: 500;
    color: var(--nav-text2);
    cursor: pointer;
    background: transparent;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    transition: border-color 0.12s, color 0.12s, background 0.12s;
  }
  .it-nav-logout:hover {
    border-color: #fca5a5;
    color: #dc2626;
    background: var(--body-err-bg);
  }

  /* HAMBURGER MOBILE */
  /* HAMBURGER BUTTON */
  .it-nav-hamburger {
    display: none;
    background: none;
    border: 0.5px solid var(--nav-border);
    border-radius: 8px;
    padding: 6px 9px;
    color: var(--nav-text2);
    cursor: pointer;
    margin-left: auto;
    transition: color 0.15s, border-color 0.15s;
  }
  .it-nav-hamburger:hover { color: var(--nav-text); border-color: var(--nav-border2, rgba(0,0,0,0.18)); }

  /* SIDEBAR OVERLAY */
  .it-sidebar-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.45);
    z-index: 1099;
    backdrop-filter: blur(2px);
    -webkit-backdrop-filter: blur(2px);
    opacity: 0;
    transition: opacity 0.25s ease;
  }
  .it-sidebar-overlay.open {
    display: block;
    opacity: 1;
  }

  /* SIDEBAR PANEL */
  .it-nav-mobile {
    position: fixed;
    top: 0;
    left: 0;
    height: 100dvh;
    width: 270px;
    max-width: 82vw;
    background: var(--nav-bg);
    border-right: 0.5px solid var(--nav-border);
    z-index: 1100;
    display: flex;
    flex-direction: column;
    transform: translateX(-100%);
    transition: transform 0.28s cubic-bezier(0.4, 0, 0.2, 1);
    overflow: hidden;
  }
  .it-nav-mobile.open {
    transform: translateX(0);
  }

  /* SIDEBAR HEADER */
  .it-sidebar-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 16px 14px;
    border-bottom: 0.5px solid var(--nav-border);
    flex-shrink: 0;
  }
  .it-sidebar-brand {
    font-size: 13px;
    font-weight: 700;
    color: var(--nav-text);
    letter-spacing: -0.01em;
    text-decoration: none;
  }
  .it-sidebar-brand span { color: #16a34a; }
  .it-sidebar-close {
    background: none;
    border: none;
    color: var(--nav-text2);
    cursor: pointer;
    padding: 4px;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.12s, color 0.12s;
  }
  .it-sidebar-close:hover { background: var(--nav-hover-bg); color: var(--nav-text); }

  /* SIDEBAR NAV LINKS */
  .it-sidebar-nav {
    flex: 1;
    overflow-y: auto;
    padding: 10px 10px 0;
  }
  .it-sidebar-nav::-webkit-scrollbar { width: 3px; }
  .it-sidebar-nav::-webkit-scrollbar-thumb { background: var(--nav-border); border-radius: 4px; }

  .it-nav-mobile a {
    display: flex;
    align-items: center;
    gap: 11px;
    padding: 10px 12px;
    border-radius: 9px;
    color: var(--nav-text2);
    font-size: 13px;
    font-weight: 500;
    text-decoration: none;
    margin-bottom: 2px;
    transition: background 0.12s, color 0.12s;
  }
  .it-nav-mobile a:hover { background: var(--nav-hover-bg); color: var(--nav-text); }
  .it-nav-mobile a.active {
    background: var(--nav-active-bg);
    color: var(--nav-text);
    font-weight: 600;
  }
  .it-nav-mobile a svg { width: 16px; height: 16px; flex-shrink: 0; }

  /* SIDEBAR SEPARATOR */
  .it-nav-mobile .m-sep {
    border: none;
    border-top: 0.5px solid var(--nav-border);
    margin: 8px 2px;
  }

  /* SIDEBAR FOOTER: user info + logout */
  .it-sidebar-footer {
    border-top: 0.5px solid var(--nav-border);
    padding: 12px 10px;
    flex-shrink: 0;
  }
  .it-nav-mobile .m-user {
    display: flex;
    align-items: center;
    gap: 9px;
    padding: 8px 12px;
    border-radius: 9px;
    background: var(--nav-active-bg);
    margin-bottom: 6px;
  }
  .m-user-info { display: flex; flex-direction: column; gap: 2px; min-width: 0; }
  .m-user-name { font-size: 12px; font-weight: 600; color: var(--nav-text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .m-user-role-wrap { display: flex; align-items: center; gap: 4px; }

  /* SIDEBAR THEME TOGGLE */
  .it-sidebar-theme {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    border-radius: 9px;
    cursor: pointer;
    background: none;
    border: none;
    color: var(--nav-text2);
    font-size: 12px;
    font-weight: 500;
    width: 100%;
    text-align: left;
    transition: background 0.12s, color 0.12s;
    margin-bottom: 4px;
  }
  .it-sidebar-theme:hover { background: var(--nav-hover-bg); color: var(--nav-text); }

  @media (max-width: 768px) {
    .it-nav-links, .it-nav-right { display: none; }
    .it-nav-hamburger { display: flex; align-items: center; }
    .it-nav { padding: 0 16px; }
  }

  /* ===== AI CHATBOX MOBILE RESPONSIVE ===== */
  @media (max-width: 480px) {
    #ai-panel {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      width: 100%;
      max-height: 75vh;
      border-radius: 20px 20px 0 0;
      border-bottom: none;
    }
    #ai-fab {
      bottom: 16px;
      right: 16px;
      width: 48px;
      height: 48px;
    }
  }
</style>

<%
  /* LOGIKA AKSES MENU YANG SUDAH DIPERKETAT */
  // 1. Cek apakah yang login adalah Admin Utama (punya hak kelola User)
  var isAdmin = currentUser && userPerms && userPerms.canUsers;
  
  // 2. KUNCI AUDIT: Jika dia dicentang Audit DAN bukan Admin Utama, kunci total menunya!
  var isAuditOnly = currentUser && userPerms && userPerms.canAudit && !isAdmin;
  
  // 3. Cek apakah user Maintenance
  var isMaintenance = currentUser && !isAdmin && (currentUser.divisi || '').toUpperCase() === 'MAINTENANCE';

  // 3b. Cek apakah user Viewer Maintenance (misal HRGA)
  var isViewerMaintenance = currentUser && !isAdmin && !isMaintenance && userPerms && userPerms.canViewMaintenance && !userPerms.canViewLog;

  // 4. Logika untuk user IT atau Admin
  var isItOrAdmin = currentUser && !isAuditOnly && !isMaintenance && !isViewerMaintenance && (
    isAdmin ||
    (currentUser.divisi && currentUser.divisi.toLowerCase() === 'it')
  );
  
  // 5. Logika untuk user divisi biasa (non-IT, non-Audit, non-Maintenance, non-ViewerMTC)
  var isNonItUser = currentUser && !isItOrAdmin && !isAuditOnly && !isMaintenance && !isViewerMaintenance && userPerms && userPerms.canAsset && !userPerms.canViewLog;
  
  var asetPublicHref = currentUser ? '/aset-public/' + encodeURIComponent(currentUser.divisi || 'IT') : '/aset-public';

  /* LOGIKA KLIK LOGO BRAND */
  var brandHref = '/kerja';
  if (isAuditOnly)            brandHref = '/audit';
  else if (isMaintenance)     brandHref = '/maintenance';
  else if (isViewerMaintenance) brandHref = '/maintenance';
  else if (isNonItUser)       brandHref = '/aset';
  else if (!currentUser)      brandHref = '/';
%>

<nav class="it-nav" id="mainNav">
  <a href="<%= brandHref %>" class="it-nav-brand">&gt;_ IT <span>SUPPORT</span> LOG</a>

  <div class="it-nav-links">
    <% if (isAuditOnly) { %>
      <!-- JIKA YANG LOGIN TIM AUDIT, KUNCI HANYA DI MENU INI -->
      <a href="/audit" class="it-nav-link <%= activePage === 'audit' ? 'active' : '' %>" style="color:#f59e0b;">
        <i class="bi bi-search"></i>
        Dashboard Audit
      </a>

    <% } else if (isNonItUser) { %>
      <a href="<%= asetPublicHref %>" class="it-nav-link <%= activePage === 'aset-public' ? 'active' : '' %>">
        <i class="bi bi-house"></i>
        Home
      </a>
      <a href="/aset" class="it-nav-link <%= activePage === 'aset' ? 'active' : '' %>">
        <i class="bi bi-grid"></i>
        Dashboard Aset
      </a>
      <% if (typeof currentUser !== 'undefined' && currentUser && userPerms && userPerms.canVendor) { %>
      <a href="/vendor" class="it-nav-link <%= activePage === 'vendor' ? 'active' : '' %>" style="color:#4da6ff;">
        <i class="bi bi-shop"></i>
        Vendor
      </a>
      <% } %>

    <% } else if (isMaintenance) { %>
      <a href="/maintenance" class="it-nav-link <%= activePage === 'maintenance' ? 'active' : '' %>" style="color:#fb923c;">
        <i class="bi bi-tools"></i>
        Dashboard Maintenance
      </a>
      <a href="/tugas" class="it-nav-link <%= activePage === 'tugas' ? 'active' : '' %>" style="color:#fbbf24;">
        <i class="bi bi-clipboard-check"></i>
        Tugas Hari Ini
      </a>

    <% } else if (isViewerMaintenance) { %>
      <a href="/maintenance" class="it-nav-link <%= activePage === 'maintenance' ? 'active' : '' %>" style="color:#67e8f9;">
        <i class="bi bi-eye"></i>
        Jurnal Maintenance
      </a>
      <a href="/tugas" class="it-nav-link <%= activePage === 'tugas' ? 'active' : '' %>" style="color:#fbbf24;">
        <i class="bi bi-clipboard-check"></i>
        Tugas Maintenance
      </a>

    <% } else { %>
      <% if (currentUser && userPerms && userPerms.canViewLog) { %>
      <a href="/kerja" class="it-nav-link <%= activePage === 'dashboard' ? 'active' : '' %>">
        <i class="bi bi-grid-1x2"></i>
        Dashboard IT
      </a>
      <% } %>
      <% if (!currentUser || (userPerms && userPerms.canAsset)) { %>
      <%
        var isAdminRole   = currentUser && userPerms && (userPerms.canUsers || userPerms.canAudit);
        var asetHref  = (!currentUser || isAdminRole) ? '/aset' : asetPublicHref;
        var asetLabel = (!currentUser || isAdminRole) ? 'Aset IT' : 'Aset ' + (currentUser.divisi || '');
      %>
      <a href="<%= asetHref %>" class="it-nav-link <%= activePage === 'aset' || activePage === 'aset-public' ? 'active' : '' %>">
        <i class="bi bi-pc-display"></i>
        <%= asetLabel %>
      </a>
      <% } %>
      <% if (typeof currentUser !== 'undefined' && currentUser && userPerms && userPerms.canVendor) { %>
      <a href="/vendor" class="it-nav-link <%= activePage === 'vendor' ? 'active' : '' %>" style="color:#4da6ff;">
        <i class="bi bi-shop"></i>
        Vendor
      </a>
      <% } %>
      <% if (typeof currentUser !== 'undefined' && currentUser && userPerms && userPerms.canUsers) { %>
      <a href="/users" class="it-nav-link <%= activePage === 'users' ? 'active' : '' %>">
        <i class="bi bi-people"></i>
        Users
      </a>
      <% } %>
      <% if (typeof currentUser !== 'undefined' && currentUser && userPerms && userPerms.canAudit) { %>
      <a href="/audit" class="it-nav-link <%= activePage === 'audit' ? 'active' : '' %>" style="color:#f59e0b;">
        <i class="bi bi-search"></i>
        Audit
      </a>
      <% } %>
      <% if (typeof currentUser !== 'undefined' && currentUser && (userPerms.canUsers || userPerms.canTugas)) { %>
      <a href="/tugas" class="it-nav-link <%= activePage === 'tugas' ? 'active' : '' %>" style="color:#fbbf24;">
        <i class="bi bi-clipboard-check"></i>
        Tugas
      </a>
      <% } %>
  </div>

  <div class="it-nav-right">
    <!-- THEME TOGGLE -->
    <button class="it-nav-theme" onclick="toggleTheme()" id="navThemeBtn">
      <div class="t-track"><div class="t-thumb" id="navThumb"></div></div>
      <span id="navThemeLabel">Light</span>
    </button>

    <div class="it-nav-divider"></div>

    <% if (typeof currentUser !== 'undefined' && currentUser) { %>
    <%
      var initials = currentUser.nama
        ? currentUser.nama.split(' ').slice(0,2).map(function(w){ return w[0]; }).join('').toUpperCase()
        : currentUser.username.slice(0,2).toUpperCase();
      var roleClass = currentUser.role === 'admin' ? 'role-admin'
                    : currentUser.role === 'user'   ? 'role-user'
                    : currentUser.role === 'viewer' ? 'role-viewer'
                    : 'role-custom';
    %>
    <!-- USER PILL + DROPDOWN -->
    <div class="it-nav-user-wrap">
      <div class="it-nav-user" onclick="toggleUserDropdown(event)">
        <div class="it-nav-avatar">
          <% if (currentUser.fotoUrl) { %>
            <img src="<%= currentUser.fotoUrl %>" alt="<%= initials %>">
          <% } else { %><%= initials %><% } %>
        </div>
        <span class="it-nav-name"><%= currentUser.nama %></span>
        <span class="it-nav-role <%= roleClass %>"><%= currentUser.role.toUpperCase() %></span>
        <i class="bi bi-chevron-down" style="opacity:0.5;font-size:10px;flex-shrink:0;"></i>
      </div>
      <div class="it-user-dropdown" id="userDropdown">
        <div class="it-dd-header">
          <div class="it-dd-name"><%= currentUser.nama %></div>
          <div class="it-dd-username">@<%= currentUser.username %></div>
        </div>
        <a href="/settings" class="it-dd-item">
          <i class="bi bi-gear"></i>
          Pengaturan Akun
        </a>
        <div class="it-dd-divider"></div>
        <a href="/logout" class="it-dd-item danger">
          <i class="bi bi-box-arrow-right"></i>
          Keluar
        </a>
      </div>
    </div>
    <% } else { %>
    <!-- TOMBOL LOGIN untuk public view -->
    <a href="/login" style="padding:6px 14px;border-radius:8px;background:var(--body-accent);color:var(--body-bg);font-size:13px;font-weight:600;text-decoration:none;white-space:nowrap;transition:opacity 0.15s;" onmouseover="this.style.opacity='0.85'" onmouseout="this.style.opacity='1'">Login</a>
    <% } %>
  </div>

  <button class="it-nav-hamburger" onclick="toggleMobileNav()">
    <i class="bi bi-list" style="font-size:18px;"></i>
  </button>
</nav>

<!-- SIDEBAR OVERLAY -->
<div class="it-sidebar-overlay" id="sidebarOverlay" onclick="closeSidebar()"></div>

<!-- SIDEBAR -->
<div class="it-nav-mobile" id="mobileNav">

  <!-- Header -->
  <div class="it-sidebar-header">
    <a href="<%= brandHref %>" class="it-sidebar-brand">&gt;_ IT <span>SUPPORT</span> LOG</a>
    <button class="it-sidebar-close" onclick="closeSidebar()" aria-label="Tutup menu">
      <i class="bi bi-x-lg"></i>
    </button>
  </div>

  <!-- Nav Links -->
  <nav class="it-sidebar-nav">
    <% if (isAuditOnly) { %>
      <a href="/audit" class="<%= activePage === 'audit' ? 'active' : '' %>" style="color:#f59e0b;" onclick="closeSidebar()">
        <i class="bi bi-search"></i>
        Dashboard Audit
      </a>
    <% } else if (isNonItUser) { %>
      <a href="<%= asetPublicHref %>" class="<%= activePage === 'aset-public' ? 'active' : '' %>" onclick="closeSidebar()">
        <i class="bi bi-house"></i>
        Home
      </a>
      <a href="/aset" class="<%= activePage === 'aset' ? 'active' : '' %>" onclick="closeSidebar()">
        <i class="bi bi-grid"></i>
        Dashboard Aset
      </a>
      <% if (typeof currentUser !== 'undefined' && currentUser && userPerms && userPerms.canVendor) { %>
      <a href="/vendor" class="<%= activePage === 'vendor' ? 'active' : '' %>" style="color:#4da6ff;" onclick="closeSidebar()">
        <i class="bi bi-shop"></i>
        Vendor
      </a>
      <% } %>
    <% } else if (isMaintenance) { %>
      <!-- MAINTENANCE USER SIDEBAR -->
      <a href="/maintenance" class="<%= activePage === 'maintenance' ? 'active' : '' %>" style="color:#fb923c;" onclick="closeSidebar()">
        <i class="bi bi-tools"></i>
        Dashboard Maintenance
      </a>
      <a href="/tugas" class="<%= activePage === 'tugas' ? 'active' : '' %>" style="color:#fbbf24;" onclick="closeSidebar()">
        <i class="bi bi-clipboard-check"></i>
        Tugas Hari Ini
      </a>
    <% } else if (isViewerMaintenance) { %>
      <!-- VIEWER MAINTENANCE SIDEBAR (HRGA, dll) -->
      <a href="/maintenance" class="<%= activePage === 'maintenance' ? 'active' : '' %>" style="color:#67e8f9;" onclick="closeSidebar()">
        <i class="bi bi-eye"></i>
        Jurnal Maintenance
      </a>
      <a href="/tugas" class="<%= activePage === 'tugas' ? 'active' : '' %>" style="color:#fbbf24;" onclick="closeSidebar()">
        <i class="bi bi-clipboard-check"></i>
        Tugas Maintenance
      </a>
    <% } else { %>
      <% if (currentUser && userPerms && userPerms.canViewLog) { %>
      <a href="/kerja" class="<%= activePage === 'dashboard' ? 'active' : '' %>" onclick="closeSidebar()">
        <i class="bi bi-grid-1x2"></i>
        Dashboard IT
      </a>
      <% } %>
      <% if (!currentUser || (userPerms && userPerms.canAsset)) { %>
      <a href="<%= asetHref %>" class="<%= activePage === 'aset' || activePage === 'aset-public' ? 'active' : '' %>" onclick="closeSidebar()">
        <i class="bi bi-pc-display"></i>
        <%= asetLabel %>
      </a>
      <% } %>
      <% if (typeof currentUser !== 'undefined' && currentUser && userPerms && userPerms.canVendor) { %>
      <a href="/vendor" class="<%= activePage === 'vendor' ? 'active' : '' %>" style="color:#4da6ff;" onclick="closeSidebar()">
        <i class="bi bi-shop"></i>
        Vendor
      </a>
      <% } %>
      <% if (typeof currentUser !== 'undefined' && currentUser && userPerms && userPerms.canUsers) { %>
      <a href="/users" class="<%= activePage === 'users' ? 'active' : '' %>" onclick="closeSidebar()">
        <i class="bi bi-people"></i>
        Users
      </a>
      <% } %>
      <% if (typeof currentUser !== 'undefined' && currentUser && userPerms && userPerms.canAudit) { %>
      <a href="/audit" class="<%= activePage === 'audit' ? 'active' : '' %>" style="color:#f59e0b;" onclick="closeSidebar()">
        <i class="bi bi-search"></i>
        Audit
      </a>
      <% } %>
    <% } %>
  </nav>

  <!-- Footer -->
  <div class="it-sidebar-footer">
    <button class="it-sidebar-theme" onclick="toggleTheme()">
      <i class="bi bi-circle-half"></i>
      <span id="sidebarThemeLabel">Light Mode</span>
    </button>

    <% if (typeof currentUser === 'undefined' || !currentUser) { %>
    <a href="/login" onclick="closeSidebar()" style="display:flex;align-items:center;justify-content:center;gap:8px;padding:11px 12px;border-radius:9px;background:var(--body-accent);color:var(--body-bg);font-size:13px;font-weight:700;text-decoration:none;transition:opacity 0.15s;" onmouseover="this.style.opacity='0.88'" onmouseout="this.style.opacity='1'">
      <i class="bi bi-box-arrow-in-right"></i>
      Login
    </a>
    <% } else { %>
    <div class="m-user">
      <div class="it-nav-avatar" style="width:36px;height:36px;font-size:13px;overflow:hidden;">
        <% if (currentUser.fotoUrl) { %>
          <img src="<%= currentUser.fotoUrl %>" alt="<%= initials %>" style="width:100%;height:100%;object-fit:cover;border-radius:50%;">
        <% } else { %><%= initials %><% } %>
      </div>
      <div class="m-user-info">
        <span class="m-user-name"><%= currentUser.nama %></span>
        <div class="m-user-role-wrap">
          <span class="it-nav-role <%= roleClass %>"><%= currentUser.role.toUpperCase() %></span>
        </div>
      </div>
    </div>
    <a href="/settings" onclick="closeSidebar()" style="display:flex;align-items:center;gap:10px;padding:9px 12px;border-radius:9px;color:var(--nav-text2);font-size:13px;font-weight:600;text-decoration:none;transition:background 0.12s;" onmouseover="this.style.background='var(--nav-hover-bg)'" onmouseout="this.style.background='none'">
      <i class="bi bi-gear"></i>
      Pengaturan Akun
    </a>
    <a href="/logout" style="display:flex;align-items:center;gap:10px;padding:9px 12px;border-radius:9px;color:#dc2626;font-size:13px;font-weight:600;text-decoration:none;transition:background 0.12s;" onmouseover="this.style.background='rgba(220,38,38,0.08)'" onmouseout="this.style.background='none'">
      <i class="bi bi-box-arrow-right"></i>
      Keluar
    </a>
    <% } %>
  </div>
</div>

<script>
(function() {
  function getTheme() { return localStorage.getItem('itlog-theme') || 'dark'; }

  function applyTheme(t) {
    document.documentElement.setAttribute('data-theme', t);
    localStorage.setItem('itlog-theme', t);
    var lbl   = document.getElementById('navThemeLabel');
    var thumb = document.getElementById('navThumb');
    if (lbl)   lbl.textContent = t === 'dark' ? 'Light' : 'Dark';
    if (thumb) {
      thumb.style.transform  = t === 'light' ? 'translateX(12px)' : '';
      thumb.style.background = t === 'light' ? '#ffffff' : '';
    }
  }

  window.toggleTheme = function() {
    applyTheme(getTheme() === 'dark' ? 'light' : 'dark');
  };

  function updateSidebarThemeLabel(t) {
    var lbl = document.getElementById('sidebarThemeLabel');
    if (lbl) lbl.textContent = t === 'dark' ? 'Light Mode' : 'Dark Mode';
  }

  window.openSidebar = function() {
    var nav = document.getElementById('mobileNav');
    var overlay = document.getElementById('sidebarOverlay');
    if (nav) nav.classList.add('open');
    if (overlay) overlay.classList.add('open');
    document.body.style.overflow = 'hidden';
  };

  window.closeSidebar = function() {
    var nav = document.getElementById('mobileNav');
    var overlay = document.getElementById('sidebarOverlay');
    if (nav) nav.classList.remove('open');
    if (overlay) overlay.classList.remove('open');
    document.body.style.overflow = '';
  };

  // Swipe left to close sidebar
  (function() {
    var startX = 0;
    var nav = document.getElementById('mobileNav');
    if (!nav) return;
    nav.addEventListener('touchstart', function(e) { startX = e.touches[0].clientX; }, { passive: true });
    nav.addEventListener('touchend', function(e) {
      if (startX - e.changedTouches[0].clientX > 60) closeSidebar();
    }, { passive: true });
  })();

  // Keep hamburger onclick pointing to openSidebar
  window.toggleMobileNav = window.openSidebar;

  applyTheme(getTheme());
  updateSidebarThemeLabel(getTheme());
})();

// User dropdown toggle
window.toggleUserDropdown = function(e) {
  e.stopPropagation();
  var dd = document.getElementById('userDropdown');
  if (dd) dd.classList.toggle('open');
};
document.addEventListener('click', function() {
  var dd = document.getElementById('userDropdown');
  if (dd) dd.classList.remove('open');
});
</script>

<!-- ============================================================ -->
<!-- RSBY-AI CHATBOT WIDGET (DYNAMIC ROLE)                        -->
<!-- ============================================================ -->
<%
  // LOGIKA PESAN SAMBUTAN AI BERDASARKAN ROLE
  let aiWelcome = "";
  let aiSub = "Powered by Gemini · Auri Steel Metalindo";
  let aiPrompts = [];

  if (typeof currentUser !== 'undefined' && currentUser) {
      let isAd = userPerms && userPerms.canUsers;
      let isAud = userPerms && userPerms.canAudit && !isAd;
      let div = (currentUser.divisi || 'IT').toUpperCase();
      let isIt = (div === 'IT' || div === 'IT & IC') && !isAd && !isAud;

      if (isAd) {
          aiWelcome = "Halo Admin <strong>" + currentUser.nama + "</strong>! 👋<br>RSBY-AI siap bantu pantau seluruh log sistem, mutasi aset, dan kelola user.";
          aiPrompts = ["📊 Rangkuman sistem hari ini", "⚠️ Aset yang sedang rusak", "🔍 Analisa log terbaru", "🏪 Cek status vendor"];
      } else if (isAud) {
          aiWelcome = "Halo Tim Audit <strong>" + currentUser.nama + "</strong>! 🕵️‍♂️<br>Mari analisa anomali log, pergerakan aset, dan evaluasi SLA hari ini.";
          aiPrompts = ["📈 Analisa anomali log bulan ini", "🔧 Aset paling sering diservis", "⏱️ Berapa rata-rata SLA perbaikan?", "⚠️ Aset dipinjam belum kembali"];
      } else if (isIt) {
          aiWelcome = "Halo Tim IT <strong>" + currentUser.nama + "</strong>! 💻<br>Ada tiket pending yang mau dieksekusi atau butuh info troubleshooting?";
          aiPrompts = ["📋 Tiket pending hari ini", "📝 Rangkuman log IT hari ini", "🖥️ Stok aset IT yang habis", "💡 Tips troubleshooting jaringan"];
      } else {
          aiWelcome = "Halo Tim <strong>" + div + "</strong> (" + currentUser.nama + ")! 👋<br>Butuh info soal ketersediaan stok atau status servis aset divisi kalian?";
          aiPrompts = ["📦 Stok aset " + div + " yang menipis?", "🔧 Aset " + div + " yang diservis", "🔑 Daftar peminjam aset " + div, "🏪 Vendor " + div + " yang tersedia"];
      }
  }
%>

<style>
  /* Floating button */
  #ai-fab {
    position: fixed; bottom: 24px; right: 24px; width: 52px; height: 52px;
    border-radius: 50%; background: linear-gradient(135deg, #6366f1, #8b5cf6);
    color: #fff; border: none; cursor: pointer;
    box-shadow: 0 4px 18px rgba(99,102,241,0.45);
    display: flex; align-items: center; justify-content: center;
    z-index: 9998; transition: transform 0.2s, box-shadow 0.2s;
  }
  #ai-fab:hover { transform: scale(1.08); box-shadow: 0 6px 24px rgba(99,102,241,0.55); }
  #ai-fab svg { width: 24px; height: 24px; }
  #ai-fab .ai-notif {
    position: absolute; top: 2px; right: 2px; width: 10px; height: 10px;
    background: #22c55e; border-radius: 50%; border: 2px solid #fff; display: none;
  }

  /* Chat panel */
  #ai-panel {
    position: fixed; bottom: 88px; right: 24px; width: 360px; max-height: 540px;
    background: var(--body-card, #fff); border: 1px solid var(--body-border, rgba(0,0,0,0.1));
    border-radius: 16px; box-shadow: 0 12px 40px rgba(0,0,0,0.18);
    display: none; flex-direction: column; z-index: 9999; overflow: hidden; font-family: inherit;
  }
  #ai-panel.open { display: flex; }

  /* Header */
  .ai-header {
    display: flex; align-items: center; gap: 10px; padding: 14px 16px;
    background: linear-gradient(135deg, #6366f1, #8b5cf6); color: #fff;
    border-radius: 16px 16px 0 0; flex-shrink: 0;
  }
  .ai-header-icon {
    width: 34px; height: 34px; background: rgba(255,255,255,0.2);
    border-radius: 50%; display: flex; align-items: center; justify-content: center; flex-shrink: 0;
  }
  .ai-header-title { font-weight: 700; font-size: 14px; line-height: 1.2; }
  .ai-header-sub { font-size: 11px; opacity: 0.8; }
  .ai-header-close {
    margin-left: auto; background: none; border: none; color: #fff;
    cursor: pointer; padding: 4px; border-radius: 6px; display: flex; align-items: center;
    opacity: 0.8; transition: opacity 0.15s;
  }
  .ai-header-close:hover { opacity: 1; background: rgba(255,255,255,0.15); }

  /* Messages area */
  #ai-messages {
    flex: 1; overflow-y: auto; padding: 14px 14px 8px; display: flex;
    flex-direction: column; gap: 10px; scroll-behavior: smooth;
  }
  #ai-messages::-webkit-scrollbar { width: 4px; }
  #ai-messages::-webkit-scrollbar-thumb { background: var(--body-border2, #ccc); border-radius: 4px; }

  .ai-msg { display: flex; gap: 8px; align-items: flex-start; animation: aiMsgIn 0.2s ease; }
  @keyframes aiMsgIn { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: translateY(0); } }
  .ai-msg.user { flex-direction: row-reverse; }
  
  .ai-bubble { max-width: 80%; padding: 9px 13px; border-radius: 14px; font-size: 13px; line-height: 1.55; word-break: break-word; white-space: pre-wrap; }
  .ai-msg.bot .ai-bubble { background: var(--body-bg3, #f3f4f6); color: var(--body-text, #111827); border-radius: 4px 14px 14px 14px; }
  .ai-msg.user .ai-bubble { background: linear-gradient(135deg, #6366f1, #8b5cf6); color: #fff; border-radius: 14px 4px 14px 14px; }
  
  .ai-avatar { width: 28px; height: 28px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 12px; font-weight: 700; flex-shrink: 0; }
  .ai-msg.bot .ai-avatar { background: linear-gradient(135deg,#6366f1,#8b5cf6); color:#fff; }
  .ai-msg.user .ai-avatar { background: var(--body-bg3,#e5e7eb); color: var(--body-text,#374151); }

  /* Typing indicator */
  .ai-typing .ai-bubble { display: flex; gap: 4px; align-items: center; padding: 12px 16px; }
  .ai-typing .dot { width: 7px; height: 7px; border-radius: 50%; background: var(--body-text2, #6b7280); animation: typingDot 1.2s infinite; }
  .ai-typing .dot:nth-child(2) { animation-delay: 0.2s; }
  .ai-typing .dot:nth-child(3) { animation-delay: 0.4s; }
  @keyframes typingDot { 0%,60%,100% { transform: translateY(0); opacity: 0.4; } 30% { transform: translateY(-5px); opacity: 1; } }

  /* Quick prompts */
  .ai-quick { display: flex; flex-wrap: wrap; gap: 6px; padding: 0 14px 10px; flex-shrink: 0; }
  .ai-quick-btn { font-size: 11px; padding: 5px 10px; border: 1px solid var(--body-border2, rgba(0,0,0,0.14)); background: var(--body-bg, #f9fafb); color: var(--body-text2, #6b7280); border-radius: 20px; cursor: pointer; transition: all 0.15s; white-space: nowrap; }
  .ai-quick-btn:hover { background: #6366f1; color: #fff; border-color: #6366f1; }

  /* Input area */
  .ai-input-area { display: flex; gap: 8px; align-items: flex-end; padding: 10px 14px 14px; border-top: 1px solid var(--body-border, rgba(0,0,0,0.08)); flex-shrink: 0; background: var(--body-card, #fff); }
  #ai-input { flex: 1; border: 1px solid var(--body-input-border, rgba(0,0,0,0.12)); background: var(--body-input, #f9fafb); color: var(--body-text, #111827); border-radius: 10px; padding: 9px 12px; font-size: 13px; resize: none; outline: none; max-height: 100px; min-height: 38px; font-family: inherit; line-height: 1.4; transition: border-color 0.15s; }
  #ai-input:focus { border-color: #6366f1; }
  #ai-send { width: 38px; height: 38px; background: linear-gradient(135deg, #6366f1, #8b5cf6); border: none; border-radius: 10px; cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; transition: opacity 0.15s, transform 0.15s; color: #fff; }
  #ai-send:hover { opacity: 0.9; transform: scale(1.05); }
  #ai-send:disabled { opacity: 0.5; cursor: not-allowed; transform: none; }
  #ai-send svg { width: 17px; height: 17px; }

  /* Foto grid */
  .ai-photo-grid { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 8px; }
  .ai-photo-item { position: relative; border-radius: 8px; overflow: hidden; border: 1px solid rgba(255,255,255,0.15); cursor: pointer; transition: transform 0.15s; }
  .ai-photo-item:hover { transform: scale(1.03); }
  .ai-photo-item img { width: 110px; height: 80px; object-fit: cover; display: block; }
  .ai-photo-label { position: absolute; bottom: 0; left: 0; right: 0; background: rgba(0,0,0,0.55); color: #fff; font-size: 9px; padding: 2px 5px; text-align: center; }
  
  #ai-lightbox { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.88); z-index: 99999; align-items: center; justify-content: center; flex-direction: column; }
  #ai-lightbox.show { display: flex; }
  #ai-lightbox img { max-width: 90vw; max-height: 80vh; border-radius: 10px; box-shadow: 0 8px 40px rgba(0,0,0,0.6); }
  #ai-lightbox-close { position: absolute; top: 18px; right: 22px; background: rgba(255,255,255,0.12); border: none; color: #fff; border-radius: 50%; width: 36px; height: 36px; font-size: 18px; cursor: pointer; display: flex; align-items: center; justify-content: center; }
  #ai-lightbox-close:hover { background: rgba(255,255,255,0.25); }
</style>

<!-- Lightbox global -->
<div id="ai-lightbox" onclick="closeAiLightbox()">
  <button id="ai-lightbox-close" onclick="closeAiLightbox()">✕</button>
  <img id="ai-lightbox-img" src="" alt="Foto Aset">
</div>

<!-- FAB Button -->
<button id="ai-fab" onclick="toggleAiPanel()" title="AI Assistant">
  <i class="bi bi-robot" style="font-size:20px;"></i>
  <span class="ai-notif" id="aiNotif"></span>
</button>

<!-- Chat Panel -->
<div id="ai-panel">
  <div class="ai-header">
    <div class="ai-header-icon">
      <i class="bi bi-robot" style="font-size:18px;"></i>
    </div>
    <div>
      <div class="ai-header-title">RSBY-AI Assistant</div>
      <div class="ai-header-sub"><%- aiSub %></div>
    </div>
    <button class="ai-header-close" onclick="toggleAiPanel()"><i class="bi bi-x-lg"></i></button>
  </div>

  <div id="ai-messages">
    <div class="ai-msg bot">
      <div class="ai-avatar">AI</div>
      <div class="ai-bubble"><%- aiWelcome %></div>
    </div>
  </div>

  <div class="ai-quick" id="aiQuickPrompts">
    <% aiPrompts.forEach(function(prompt) { %>
      <button class="ai-quick-btn" onclick="sendQuick('<%= prompt %>')"><%= prompt %></button>
    <% }); %>
  </div>

  <div class="ai-input-area">
    <textarea id="ai-input" placeholder="Tanya sesuatu..." rows="1" onkeydown="aiKeydown(event)" oninput="autoResize(this)"></textarea>
    <button id="ai-send" onclick="sendAiMessage()">
      <i class="bi bi-send-fill"></i>
    </button>
  </div>
</div>

<script>
(function() {
  var aiHistory = [];
  var aiOpen = false;

  window.toggleAiPanel = function() {
    aiOpen = !aiOpen;
    var panel = document.getElementById('ai-panel');
    if (aiOpen) { panel.classList.add('open'); document.getElementById('ai-input').focus(); document.getElementById('aiNotif').style.display = 'none'; } 
    else { panel.classList.remove('open'); }
  };

  window.autoResize = function(el) { el.style.height = 'auto'; el.style.height = Math.min(el.scrollHeight, 100) + 'px'; };
  window.aiKeydown = function(e) { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendAiMessage(); } };
  window.sendQuick = function(msg) { document.getElementById('ai-input').value = msg; sendAiMessage(); document.getElementById('aiQuickPrompts').style.display = 'none'; };

  function escHtml(s) {
    var html = s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
    html = html.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>');
    html = html.replace(/\*(.*?)\*/g, '<em>$1</em>');
    html = html.replace(/\n/g,'<br>');
    return html;
  }

  function appendMsg(role, text, photos) {
    var msgs = document.getElementById('ai-messages'); var div = document.createElement('div'); div.className = 'ai-msg ' + role;
    var avatar = role === 'bot' ? 'AI' : '<%= typeof currentUser !== "undefined" && currentUser ? currentUser.nama.substring(0,2).toUpperCase() : "U" %>';
    var bubbleHtml = '<div class="ai-bubble">' + escHtml(text);
    if (photos && photos.length > 0) {
      bubbleHtml += '<div class="ai-photo-grid">';
      photos.forEach(function(url, idx) {
        bubbleHtml += '<div class="ai-photo-item" id="aphoto-' + idx + '" onclick="openAiLightbox(\'' + escAttr(url) + '\')"><img src="' + escAttr(url) + '" loading="lazy" onerror="this.closest(\'.ai-photo-item\').style.display=\'none\'"><div class="ai-photo-label">📷 Foto ' + (idx+1) + '</div></div>';
      });
      bubbleHtml += '</div>';
    }
    bubbleHtml += '</div>';
    div.innerHTML = '<div class="ai-avatar">' + avatar + '</div>' + bubbleHtml;
    msgs.appendChild(div); msgs.scrollTop = msgs.scrollHeight; return div;
  }

  window.openAiLightbox = function(url) { document.getElementById('ai-lightbox-img').src = url; document.getElementById('ai-lightbox').classList.add('show'); };
  window.closeAiLightbox = function() { document.getElementById('ai-lightbox').classList.remove('show'); document.getElementById('ai-lightbox-img').src = ''; };
  document.addEventListener('keydown', function(e) { if (e.key === 'Escape') closeAiLightbox(); });
  function escAttr(s) { return s.replace(/"/g, '&quot;').replace(/'/g, '&#39;'); }
  function showTyping() { var msgs = document.getElementById('ai-messages'); var div = document.createElement('div'); div.className = 'ai-msg bot ai-typing'; div.id = 'ai-typing-indicator'; div.innerHTML = '<div class="ai-avatar">AI</div><div class="ai-bubble"><span class="dot"></span><span class="dot"></span><span class="dot"></span></div>'; msgs.appendChild(div); msgs.scrollTop = msgs.scrollHeight; }
  function removeTyping() { var t = document.getElementById('ai-typing-indicator'); if (t) t.remove(); }

  window.sendAiMessage = async function() {
    var input = document.getElementById('ai-input'); var msg = input.value.trim(); if (!msg) return;
    var sendBtn = document.getElementById('ai-send'); input.value = ''; input.style.height = 'auto'; sendBtn.disabled = true;
    appendMsg('user', msg); showTyping(); document.getElementById('aiQuickPrompts').style.display = 'none';

    try {
      var res = await fetch('/api/ai-chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message: msg, history: aiHistory }) });
      var data = await res.json(); removeTyping();
      var reply = data.reply || data.error || 'Tidak ada respons.';
      appendMsg('bot', reply, data.photos || []);
      aiHistory.push({ role: 'user', content: msg }); aiHistory.push({ role: 'assistant', content: reply });
      if (aiHistory.length > 20) aiHistory = aiHistory.slice(-20);
    } catch(err) { removeTyping(); appendMsg('bot', '⚠️ Gagal terhubung ke AI. Cek koneksi.'); } 
    finally { sendBtn.disabled = false; input.focus(); }
  };
})();
</script>